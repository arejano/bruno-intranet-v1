import { Box } from "@chakra-ui/react";

import HomeIntranet from "components/HomeIntranet";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <Box mt={32} w="full">
        <HomeIntranet />
      </Box>
    </Box>

  );
};

export default Home;
